# Runtime Project Fixture

This immutable project exercises one selected runtime identifier and a compact
ordered runtime-identifier expansion batch. It has no sources or package
references so the evaluation benchmark isolates project parsing and target
dimension materialization.
