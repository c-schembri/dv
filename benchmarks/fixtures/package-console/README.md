# Package Console Fixture

This exact-version `net10.0` fixture exercises one direct package with no
dependencies in its selected `net6.0` compatibility group. It selects one
compile and runtime assembly from `Newtonsoft.Json` `13.0.3`.

Benchmarks must copy this directory before creating `obj/`, `dv.lock.json`, or
an isolated global-packages cache.
