# Unavailable pack fixture

This `net10.0` self-contained project requests the SDK-recognized `linux-arm`
RID. The isolated package cache and intentionally empty local source force an
actionable missing-runtime-pack diagnostic without network access.
