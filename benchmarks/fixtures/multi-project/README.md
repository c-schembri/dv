# Multi-Project Fixture

This fixture contains a three-node project graph with one shared dependency.
It is the next input after `small-console` for project discovery, graph
construction, scheduling, incremental invalidation, and deterministic ordering.

Build `App/App.csproj` as the graph root.
